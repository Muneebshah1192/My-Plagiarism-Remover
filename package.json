{
  "name": "originality-assistant-netlify-fixed",
  "version": "4.0.0",
  "private": true,
  "description": "Netlify-ready originality assistant with zero npm dependencies, local rewrite engine, and similarity scoring.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "start": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": "20.x",
    "npm": ">=10 <11"
  }
}
